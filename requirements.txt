# Django core
Django>=6.0

# Database driver (SQLite used by default; install mysqlclient if switching to MySQL)
# mysqlclient>=2.2.0

# Django REST Framework (for APIs)
djangorestframework>=3.16.0

# OAuth2 Authorization Server + JWT
django-oauth-toolkit>=3.0.0
PyJWT>=2.8.0
cryptography>=42.0.0

# Google OAuth (Sign in with Google)
google-auth>=2.29.0

# ML / Data processing
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.8.0
scipy>=1.10.0
joblib>=1.3.2

# Environment management
python-dotenv>=1.0.0

# HTTP / API utilities
requests>=2.32.0

# OTP generation / validation
pyotp>=2.9.0
